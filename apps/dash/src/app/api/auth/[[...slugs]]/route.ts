import { authServer } from "@omed/auth";

export const GET = authServer.handle;
export const PUT = authServer.handle;
export const POST = authServer.handle;
export const DELETE = authServer.handle;
export const OPTIONS = authServer.handle;
